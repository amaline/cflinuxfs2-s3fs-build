machine:
  services:
    - docker

dependencies:
  override:
     - docker info
     - docker build -t build-img .
     - docker create --name build-cont build-img
     - docker cp build-cont:/usr/local/bin/s3fs $CIRCLE_ARTIFACTS/s3fs